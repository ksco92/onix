[toolchain]
# Pinned for reproducible gates (coverage/lint results can drift across
# versions). Bump deliberately, in its own commit, with `make check` green.
channel = "1.98.0"
components = ["clippy", "rustfmt", "llvm-tools-preview"]
